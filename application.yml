spring:
  application:
    name: coffeeconfigserver
  profiles:
    active:
    - native
  cloud:
    config:
      server:
        native:
          search-locations:
          - "classpath:/"
server:
  port: 8071
